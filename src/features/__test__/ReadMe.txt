Each feature or domain of the application can be self-contained with its own components, styles, hooks, etc.

Purpose: For grouping components and related code for a specific feature.

/features
 ├── /Auth
 │    ├── /components
 │    ├── /hooks
 │    └── AuthPage.tsx
 └── /UserProfile
      ├── /components
      └── UserProfile.tsx