import React from 'react';

const Layout: React.FC = () => {
    return (
        <header className="app-header">
            <h1>My React App</h1>
        </header>
    );
};

export default Layout;
